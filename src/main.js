const { Todo } = require('./todos.js');
const display = require('./display.js');

const todo = new Todo("Make this app work", "Make it follow the specification. Work work work", "2025-11-01", 5, "Todo App");

console.log(todo);